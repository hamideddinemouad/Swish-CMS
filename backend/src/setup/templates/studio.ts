import type { DefaultPageTemplate } from '../../pages/defaultContent/types';
import { DEFAULT_PAGE_SEEDS } from '../default-page-seeds';

export const studioTemplate = {
  id: 'studio',
  label: 'Studio',
  seeds: DEFAULT_PAGE_SEEDS,
} satisfies DefaultPageTemplate;
